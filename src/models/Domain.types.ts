export interface IDomain {
    id: number,
    domain: string,
    virtual_uid: number,
    virtual_gid: number,
    description: string,
    enable: boolean,
    created_at: string,
    updated_at: string
}